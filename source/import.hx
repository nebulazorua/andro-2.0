import funkin.dependencies.*; // these are used pretty much everywhere so makes sense to put this here
import funkin.dependencies.states.*; // these are used pretty much everywhere so makes sense to put this here
#if desktop
import funkin.data.Discord.DiscordClient;
#end
import funkin.dependencies.Conductor.BPMChangeEvent;
import funkin.dependencies.input.*;